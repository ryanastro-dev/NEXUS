name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - master
      - work

jobs:
  rust:
    name: Rust checks
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2

      # Keep CI green while gradually paying down existing lint/style debt.
      - name: Clippy (non-blocking lint baseline)
        run: cargo clippy -p nexus-core --all-targets

      - name: Check all targets
        run: cargo check -p nexus-core --all-targets

      - name: Unit tests
        run: cargo test -p nexus-core --lib

  frontend:
    name: Frontend build
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: apps/nexus-desktop
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: apps/nexus-desktop/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
